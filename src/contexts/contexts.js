// importing createcontext, so we can use useContext

import { createContext } from "react"
// export and define our contexts
export const ProductsContext = createContext()
export const FeaturedProductsContext = createContext()
export const SquareProductsContext = createContext()